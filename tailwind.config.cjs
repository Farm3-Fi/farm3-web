const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    fontFamily: {
      'sans': ['Montserrat Alternates','ui-sans-serif']
    },
    extend: {},
  },

  plugins: [],
};

module.exports = config;
